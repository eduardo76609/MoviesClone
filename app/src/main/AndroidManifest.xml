<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moviesclone">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MoviesCloneApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviesClone">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature_serie_details.presentation.SerieDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.NoActionBar" />

        <activity
            android:name=".feature_movie_details.presentation.MovieDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.NoActionBar" />

    </application>

</manifest>